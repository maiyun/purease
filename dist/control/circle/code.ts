export const code = {
    'template': '',
    'props': {
        /** ---  'default' | 'primary' | 'info' | 'warning' | 'danger' | 'pe' --- */
        'type': {
            'default': 'default',
        },
        'plain': {
            'default': false,
        },
        /** --- 'l'| 'm' | 's' | 'xs' | 'xxs' --- */
        'size': {
            'default': 'xxs',
        },
    },
};
